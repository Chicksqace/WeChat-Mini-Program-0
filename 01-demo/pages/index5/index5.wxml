<view wx:for="{{arr}}" wx:key="{{index}}">
  {{index}}:{{item}}
</view>
<view wx:for="{{person}}" wx:for-index="key"  wx:key="{{index}}" >
  {{key}}:{{item}}
</view>
<block wx:for="{{arr}}" wx:key="{{index}}">
  {{index}}:{{item}}
</block>
